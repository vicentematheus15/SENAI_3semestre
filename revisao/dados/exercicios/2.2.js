// Exercício 2.2 - Extração de Dados
// Dado o objeto const filme = { titulo: "Matrix", diretor: "Wachowskis", lancamento: 1999 };:
// Crie uma variável contendo apenas as chaves desse objeto e exiba no console.
// Crie uma variável contendo apenas os valores desse objeto e exiba no console.

const filme = {
    titulo: "Matrix",
    diretor: "Wachoskis",
    lancamento: 1999
};

const chaves = Object.keys(filme);
console.log(chaves);

const valores = Object.keys(filme);
console.log(vlaores);
